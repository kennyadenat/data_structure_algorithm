exports.arraysTest = () => {


  let twentyThree = 'XIYYFY';
  let romanNumeral = ['YUTYYE', 'YTYYGD'];

  //add items use push and unshift

  //remove items use pop and shift
  let newShift = romanNumeral.shift();
  console.log(`this is the new shift ${romanNumeral}`);
  console.log(`this is the new shift ${newShift}`);

  // let newPop = romanNumeral.shift();
  // console.log(`this is the new pop ${romanNumeral}`);
  // console.log(`this is the new pop ${newPop}`);

  // remove item using splice

  // add items using splice

  // copy an array using slice


  // copy an array using spread operator

  // check for presence using indexof


}